<div class="top" align="center"><%= link_to_close_redbox("Close") %></div>
<pre class="rescont"><% form_for(appointment) do |f| %>
	<%= f.calendar_date_select :interview_scheduled, :embedded => true %>
	<%= f.submit "Submit" %>
<% end %>
</pre>
<div class="bottom" align="center"><%= link_to_close_redbox("Close") %></div>