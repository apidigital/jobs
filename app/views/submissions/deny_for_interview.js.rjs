page.replace_html "deny_#{@submission.id}", "#{link_to_remote_redbox(image_tag('../../images/accept.png', :title => 'Approve submission'), link_to_remote_options = {:url => {:controller => 'submissions', :action => :appointment, :id => @submission.id}})}"