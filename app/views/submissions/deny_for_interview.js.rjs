page.replace_html "accept_#{@submission.id}", "#{link_to_remote_redbox(image_tag('../../images/Crystal_Clear_action_apply.png', :size => '20x20', :title => 'Approve submission'), link_to_remote_options = {:url => {:controller => 'submissions', :action => :appointment, :id => @submission.id}})}"
page.replace_html "status#{@submission.id}", "Denied"