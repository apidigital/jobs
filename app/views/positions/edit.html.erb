<%= render :partial => 'form' %>
<!--
<h1>Editing position</h1>

<% form_for(@position) do |f| %>
  <%= f.error_messages %>

  <p>
		<%= f.label :name %><br />
    <%= f.text_field :name %>
  </p>
  <p>
		<%= f.label :description %><br />
    <%= f.text_area :description %>
  </p>
  <p>
	<%= f.collection_select(:category_id, @categories, :id, :name) %>
  </p>
	<p><label for="active">Active?:</label>
<%= f.check_box 'active' %></p>
<%= link_to "Formatting Help", "http://hobix.com/textile/quick.html"%>
  <br /><hr><br />
	<h1>Questions</h1><br />
  <p>
  <div id="questions">
		<%#= render :partial => 'questions/form', :collection => @questions %>
		<%#= sortable_element 'question', :url => {:controller => "questions", :action => "sort_questions", :id => @position.id}%>
	</div>
	</p>
	<p>
		<%#= add_task_link "New Question" %>
	</p>
		
  <p>
    <%= f.submit "Update" %>
  </p>
<% end %>
<%= link_to 'Show', @position %> |
<%= link_to 'Back', positions_path %>
-->